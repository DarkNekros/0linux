# /boot/menu.lst : fichier de configuration pour GRUB

# Voici les correspondances entre les volumes de Linux et ceux de GRUB.
# Il faut garder à l'esprit que Linux numérote à partir de 1 et GRUB
# à partir de 0 :
#
# Linux : /dev/ suivi de <type><disque a-z><partition 1-x>
# GRUB  : (     suivi de <type><disque 0-x><partition 0-x>)

# Exemples de conversions Linux -> GRUB :
# /dev/sda  -> (hd0)
# /dev/sda1 -> (hd0,0)
# /dev/sdb2 -> (hd1,1)
# /dev/sda3 -> (hd0,2)
#

# On laisse 5 secondes à l'utilisateur pour se décider :
timeout 5

# On amorcera la première entrée par défaut, GRUB commençant par 0 :
default 0

# Couleurs :
# Avant-plan/arrière-plan (normaux) puis avant-plan/arrière-plan (sélectionnés)
color green/black light-green/black
